<title>Artist</title>


<body>
  <div class="container">
    <%- include('partials/header') %>
    <h1><%= artist.name %></h1>
    <div class= "jumbotron">
      <%#forEach() acts as a for loop and the i represents the current iteration%>
      <% album.forEach(function(album, i){ %>
        <div class="row">
          <div class="albumImage well col-xs-6">
            <p>Name: <a href="../albums/<%= album.id %>"><%= album.title %></a></p>
            <p>Year: <%= album.year %></p>
          </div>

          <div class="info col-xs-6">
            <p>Album Genre: <%= artist.genre %></p>
            <p>Number of songs: <%= songCount[i] %></p>
          </div>

  </div>
  <% }) %>
  </div>
</div>
</body>

</html>
